/** Cardinal side used by endpoints and edge path routing. */
export enum Position {
    /** Top side of a node or endpoint. */
    Top,
    /** Right side of a node or endpoint. */
    Right,
    /** Bottom side of a node or endpoint. */
    Bottom,
    /** Left side of a node or endpoint. */
    Left
}
